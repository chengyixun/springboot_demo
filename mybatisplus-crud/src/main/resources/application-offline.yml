spring:
  datasource:
    druid:
      driver-class-name: com.mysql.cj.jdbc.Driver
      url: jdbc:mysql://127.0.0.1:3306/mp_crud?useUnicode=true&characterEncoding=utf-8&useSSL=false&rewriteBatchedStatements=true
      username: root
      password: root
      # 事务查询超时时间
      transaction-query-timeout: 6000
      query-timeout: 6000
      #最小空闲连接数
      initial-size: 8
  # 本地redis没有密码
  redis:
    host: 127.0.0.1
    port: 6379

    # bootstrap:
    # namespaces: application

apollo:
  cacheDir: /Users/amy
  meta: http://106.54.227.205:8080
mpc:
  common:
    algorithmCloudUrl: https://127.0.0.1:8080/offline


